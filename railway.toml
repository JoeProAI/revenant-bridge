[build]
builder = "NIXPACKS"
nixPkgs = ["nodejs-22", "python3"]
buildCommand = "npm install"

[deploy]
startCommand = "npm start"
healthcheckPath = "/test-flow"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3